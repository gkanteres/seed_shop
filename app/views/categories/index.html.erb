<h1>This is the category#index page</h1>

<% @categories.each do |category| %>
  <%= link_to category.name, category_path(category) %><br />
<% end %>
<br />

<%= link_to 'New Category', new_category_path %>
